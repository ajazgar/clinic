doctype html
html(lang='pl')
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale-1')
    link(rel='stylesheet', href='/stylesheets/style.css')
    title Appointments
  body
    script.
      function redirectToPanel() {
      var idLekarza = location.search.split('idLekarza=')[1];
      var url= '/panel?idLekarza=' + idLekarza;
      window.location.replace(url);
      }
      
    header
      main
        .content-buttons
          a.button(href='/' style="background-color: #6A2E2E;") Wyloguj się
          a.button(onclick='redirectToPanel()', style="background-color: #6A2E2E;") Wróć
        #search
          form#searchfield(action='listofappointments')
            input(type='text', name='userToSearch', required='required', style='display: inline-block;')
            input(type='submit', value='Search', style='display: inline-block;')
          h3#usersheader Appointments
          br
        ol#listofusers
          each val in values
            li  id:#{val.idWizyty}, data:#{val.Data}, domowa:#{val.Domowa}, płatna:#{val.Platna}, idLekarza:#{val.idLekarza}, idPacjenta:#{val.idPacjenta}
            a.button#userbutton(href='/deleteAppointment?login=admin&userToDelete=' + val.idWizyty) Usuń